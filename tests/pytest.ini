[pytest]
testpaths = unit integration
asyncio_mode = auto
addopts = -ra
# e2e/smoke_live.py is an opt-in script, intentionally not collected
# (it does not match test_*.py and lives outside testpaths).
